
#include <iostream>
#include <iomanip>
#include <windows.h>

using namespace std; 
int main()
{
    float y, Sum, x;
    cout << "input x";
    cin >> x;
    y = 0;
    for (int i = 1; i < 4; i++)
    {
        y += i;

        y = log(Sum * i) + (pow(x, 2));
        cout << 'y=' << y << endl;
    }
}

